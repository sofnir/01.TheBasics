import { TurnGreenDirective } from './turn-green.directive';

describe('TurnGreenDirective', () => {
  it('should create an instance', () => {
    const directive = new TurnGreenDirective();
    expect(directive).toBeTruthy();
  });
});
